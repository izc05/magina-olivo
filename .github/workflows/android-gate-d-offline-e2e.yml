name: Android Gate D Offline E2E

on:
  workflow_dispatch:
  push:
    branches:
      - "feat/android-sync-gate-d-v1"
    paths:
      - "app/src/main/java/com/isivolt/maginaolivo/data/local/**"
      - "app/src/main/java/com/isivolt/maginaolivo/data/repository/**"
      - "app/src/main/java/com/isivolt/maginaolivo/data/sync/**"
      - "app/src/androidTest/java/com/isivolt/maginaolivo/data/sync/**"
      - ".github/workflows/android-gate-d-offline-e2e.yml"
  pull_request:
    paths:
      - "app/src/main/java/com/isivolt/maginaolivo/data/local/**"
      - "app/src/main/java/com/isivolt/maginaolivo/data/repository/**"
      - "app/src/main/java/com/isivolt/maginaolivo/data/sync/**"
      - "app/src/androidTest/java/com/isivolt/maginaolivo/data/sync/**"
      - ".github/workflows/android-gate-d-offline-e2e.yml"

concurrency:
  group: android-gate-d-offline-e2e-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  e2e:
    runs-on: ubuntu-latest
    timeout-minutes: 25

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Java 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "17"

      - name: Android command-line tools
        uses: android-actions/setup-android@v4
        with:
          packages: ""

      - name: Gradle 9.6
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "9.6.0"

      - name: Enable KVM
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' |
            sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm

      - name: Run Gate D offline outbox E2E
        uses: reactivecircus/android-emulator-runner@v2
        with:
          api-level: 35
          arch: x86_64
          profile: pixel_6
          disable-animations: true
          script: >
            gradle :app:connectedDebugAndroidTest
            -Pandroid.testInstrumentationRunnerArguments.class=com.isivolt.maginaolivo.data.sync.LocalOutboxE2ETest
            --stacktrace

      - name: Upload Gate D reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: android-gate-d-offline-e2e-reports
          if-no-files-found: ignore
          path: |
            app/build/reports/androidTests/connected/
            app/build/outputs/androidTest-results/connected/
